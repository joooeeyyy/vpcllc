import { createContext } from "react";

export const ServicesContext = createContext(null);

export const MenuContext = createContext(false);

export const SelectedContext = createContext("Category");
